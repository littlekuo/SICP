#lang racket

;   applicative-order -- evaluate the arguments and then apply,
; this expression will never terminates  
;   normal-order -- would not evaluate the operands until their values were needed,
; final 0 